query Jobs($id: ID!) {
  application(id: $id) {
    id
    created
    email
    externals {
      url
    }
    job {
      position
      location
      # type
    }
    avatarUrl
    visaStatus
    # messageStatus
  }
}
